---
title: 'Read a form: its public config, or the document behind it'
description: Without `view`, this answers the published form exactly as a
  respondent receives it. A form you have just created has no published version
  yet, so it answers 404 `not_published` until you publish it; pass
  `view=document` to read the working draft instead.
full: true
_openapi:
  preload:
    - chatform
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: Without `view`, this answers the published form exactly as a respondent
          receives it. A form you have just created has no published version
          yet, so it answers 404 `not_published` until you publish it; pass
          `view=document` to read the working draft instead.
llmsExclude: true
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="chatform" operations={[{"path":"/v1/forms/{id}","method":"get"}]} />
    </>
  );
}